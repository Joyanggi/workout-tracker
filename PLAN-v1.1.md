# v1.1 작업 계획 (2026-07-31)

> 사용자 요구 3건 + 리뷰어 제안 4건. 각 항목에 현황 → 변경 → 수용 기준.
> 작업 전 DESIGN.md·REVIEW.md 참조. 완료 시 테스트 추가 필수 (기존 175개 유지).

---

## T1. 종목별 자극 팁 한 줄 (사용자 요구)

**현황**: `exercises.json`에 name/shortName/compensationSigns만 있음. ExerciseCard 펼침 상태에 루틴 note(`routineExercise.note`)는 표시되지만 자극 요령은 없음.

**변경**:
1. `Exercise` 타입에 `cueTip: string` 추가 (`src/types.ts`)
2. `exercises.json`에 아래 문안 그대로 추가 (17종목 — 문안은 루틴 설계 기준으로 확정된 것이니 임의 수정 금지)
3. `ExerciseCard` 펼침 상태의 note 아래에 💡 프리픽스로 표시. B그룹 종목이 특히 중요하므로 항상 노출 (접기 불필요 — 한 줄이라 공간 부담 없음)
4. 카탈로그는 seed가 매 부팅 bulkPut하므로 (P2-12 수정 완료) 버전 올림 불필요

| exerciseId | cueTip |
|---|---|
| incline-chest-press | 견갑을 시트에 붙이고 가슴을 편 상태 유지 — 밀 때 팔꿈치를 끝까지 펴지 말고 가슴에 긴장을 남긴다 |
| pec-deck-fly | 손이 아니라 팔꿈치 안쪽으로 민다는 느낌. 벌릴 때 3초, 가슴 바깥이 늘어나는 지점에서 1초 멈춤 — 자극은 거기서 난다 |
| flat-chest-press | 어깨를 아래로 누른 채(으쓱 금지) 가슴 중앙으로 모은다는 느낌으로 민다 |
| shoulder-press | 허리를 젖히지 말고 팔꿈치를 살짝 앞에 둔다 — 등받이에서 등이 뜨면 무게가 과한 것 |
| lateral-raise | 덤벨을 위가 아니라 멀리 옆으로 던진다는 느낌 — 새끼손가락이 아니라 팔꿈치가 먼저 올라간다. 어깨가 귀 쪽으로 오르면 무게 과함 |
| lat-pulldown | 가슴을 들고 견갑을 먼저 아래로 내린 뒤 팔꿈치를 뒤·아래로 — 손은 고리일 뿐, 쥐어짜지 않는다 |
| neutral-lat-pulldown | 팔꿈치를 몸통 옆구리로 끌어내린다는 느낌, 수축 지점에서 1초 |
| seated-cable-row | 몸통 고정, 팔꿈치로 배꼽 뒤를 찌른다는 느낌 — 어깨가 앞으로 딸려나가지 않게 |
| arm-pulldown | 팔꿈치를 편 채 광배로 눌러 내린다 — 이 동작에서 느껴지는 곳이 광배다. 랫풀 전에 그 위치를 기억해둘 것 |
| rear-delt-fly | 팔꿈치 각도를 고정하고 손이 아니라 팔꿈치를 뒤로 벌린다 — 승모가 먼저 조이면 무게를 내린다 |
| curl | 팔꿈치를 옆구리에 고정 — 올릴 때 팔꿈치가 앞으로 나오거나 어깨가 따라 올라가면 이두에서 새는 것 |
| pushdown | 팔꿈치를 옆구리에 붙여 고정하고 마지막 지점에서 삼두를 1초 짜낸다 |
| leg-extension | 올릴 때 허벅지 앞이 조이는 걸 확인하고, 내릴 때 무게에 끌려가지 말고 버틴다 |
| leg-curl | 엉덩이가 들리지 않게 — 수축 지점에서 1초 정지 |
| hip-abduction | 상체 반동 없이, 엉덩이 옆이 조이는 지점에서 1초 |
| crunch | 허리를 접는 게 아니라 갈비뼈를 골반 쪽으로 만다는 느낌 |
| dead-bug | 허리로 바닥을 누른 상태를 유지 — 허리가 뜨면 팔다리 가동범위를 줄인다 |

**수용 기준**: 세션 화면에서 카드를 펼치면 팁이 보인다. 기존 사용자도 앱 재시작만으로 반영된다.

## T2. 숫자 입력 제한 — 무게 소수점 2자리 / 횟수 정수 (사용자 요구)

**현황**: `NumberStepper`가 커밋(blur)과 스테퍼 증감 시점에는 `Math.round(×100)/100`으로 이미 2자리 반올림함. 그러나 **타이핑 중에는 자유 텍스트**라 "40.123456"이 그대로 보이고, 횟수 필드에도 소수점 입력이 가능함.

**변경** (`src/components/NumberStepper.tsx` 단일 파일):
1. `decimals?: 0 | 2` prop 추가 (기본 0)
2. `onChange` 핸들러에서 입력 패턴 검사 — 통과 못 하면 이전 text 유지 (타이핑 자체가 안 됨):
   - `decimals === 2`: `/^\d{0,4}([.,]\d{0,2})?$/`
   - `decimals === 0`: `/^\d{0,4}$/`
3. 커밋 시 반올림은 현행 유지 (이중 방어)
4. 호출부: 무게 스테퍼 `decimals={2}`, 횟수 스테퍼 `decimals={0}` (`ExerciseCard.tsx`)

**수용 기준**: 무게 필드에 셋째 자리 소수·문자를 타이핑할 수 없다. 횟수 필드에 소수점을 타이핑할 수 없다. 테스트: 패턴 함수 단위 테스트 추가.

## T3. Phase 전환 조건 자동 감지 + 원탭 전환 (사용자 질문 → 개선)

**현황**: Phase는 설정에서 수동 전환. Phase 0 조건(주 3회 × 8주)만 `phase0Progress`로 계산되고, 달성 시 홈에 문구만 표시. **Phase 1→2, 2→3 조건은 계산되지 않음.**

**방침 — 완전 자동 전환은 하지 않는다.** 루틴 문서의 전환 조건에 주관 판단("안정적으로", "대부분")이 섞여 있고, 문서가 전환을 사용자 결정으로 규정한다. 대신 **조건 충족을 감지해 배너로 제안하고, 배너에서 원탭 전환.**

**변경**:
1. `src/lib/phaseReadiness.ts` 신설 — `phaseReadiness(sessions, routine, currentPhase, today)` → 조건별 `{ label, met, detail }[]` + `allMet`. 조건의 계량화 (루틴 문서 9장 → 코드):

   **0→1**: 기존 `phase0Progress.achieved` 재사용.

   **1→2** (문서: "B그룹 절반 이상에서 감각 2점 이상이 안정적으로"):
   - 최근 4주에 기록이 있는 B그룹 recordKey 중, **최근 3회 감각 점수 중앙값 ≥ 2**인 것이 절반 이상

   **2→3** (문서 4개 조건):
   - ① 주요 3종목(`keyARecordKeys`) 각각 **무게 증가 이벤트 누적 ≥ 3회** (세션 히스토리에서 같은 recordKey의 최고 무게가 이전 최고보다 오른 횟수로 산출 — Phase 시작일 저장 없이 파생 가능)
   - ② B그룹 recordKey 2/3 이상에서 최근 4주 감각 중앙값 ≥ 2
   - ③ 최근 4주 전 세션에서 compensation == "없음" 유지
   - ④ 최근 6개월 내 4주+ 공백 없음
2. 홈 배너 추가 — 우선순위: **복귀 > 디로드 > Phase 전환 제안 > 증량 배지** (기존 게이트 패턴 따를 것). 배너: "Phase N 조건을 모두 충족했어요" + [Phase N+1로 전환] + [나중에] (dismiss는 `store/ui.ts` 패턴)
3. 설정 화면 Phase 섹션에 조건별 체크리스트 표시 (`phaseReadiness` 재사용 — 뭐가 남았는지 보이게)
4. `Settings`에 `phaseChangedAt?: Record<Phase, string>` 추가 — 전환 이력 (통계·내보내기용, 필수는 아님)

**수용 기준**: 각 조건 평가 함수에 단위 테스트 (충족/미충족/데이터 부족 케이스). 데이터가 부족하면 `met: false` + "기록 부족"으로 표시하고 배너는 뜨지 않는다.

---

## 리뷰어 제안 (시중 앱 대비 부재 기능 중 이 앱의 목적에 맞는 것만)

선정 기준: 입력 마찰을 늘리지 않고, 기록·분석 루프를 강화하는 것. 소셜·영상·플레이트 계산기 등은 목적 밖이라 제외.

## T4. 머신 세팅 메모 (권장 ★★★ — 시중 앱의 exercise note에 해당)

**무엇**: recordKey별 고정 메모 한 줄 — "시트 3칸, 등받이 2" 같은 머신 세팅. 시중 앱(Hevy, Strong)에서 사용 빈도가 가장 높은 기능 중 하나고, 머신 12종 고정 루틴인 이 앱에선 가치가 더 큼. 매 세션 세팅을 다시 찾는 시간이 사라짐.

**변경**: Dexie에 `exerciseNotes` 테이블 (`{ recordKey, note }`) 추가 (스키마 version 2 — 마이그레이션 규칙은 db/index.ts 주석 참조). ExerciseCard 펼침 상태에 연필 아이콘 → 인라인 입력. 다음 세션에도 같은 recordKey면 표시. 내보내기 MD에는 포함하지 않음 (세팅값은 분석 대상이 아님). JSON 백업에는 포함.

## T5. PR(개인 기록) 감지 (권장 ★★)

**무엇**: 세션 종료 요약에서 recordKey별 **무게 PR / e1RM PR / 반복수 PR**(같은 무게 최다 반복)을 감지해 뱃지 표시. 감량기에는 증량 조건이 잘 안 뜨므로 (§7 진전 지표 프레이밍) e1RM·반복 PR이 진전을 보여주는 주 채널이 됨. 시중 앱 전반의 표준 기능.

**변경**: `src/lib/pr.ts` 신설 (파생 계산 — 저장 없음), SummaryScreen·FinishSheet에 뱃지. 과거 전체 히스토리 대비 비교. 디로드·복귀 세션은 PR 판정 제외 (세트 수가 달라 왜곡).

## T6. 휴식 종료 로컬 알림 (권장 ★★ — 화면 꺼진 상태 대응)

**무엇**: 현재 휴식 타이머 종료 알림은 화면이 켜져 있을 때만(비프+진동 시늉). 헬스장에서 휴식 중 폰을 내려놓으면 종료를 놓침. iOS 16.4+ 홈화면 PWA는 Notification API를 지원하므로 로컬 알림 가능.

**변경**: 설정에 "휴식 종료 알림" 토글 (기본 꺼짐 — 권한 요청은 토글 시점에만, DESIGN §5.2의 권한 UX 우려 존중). 켜면 타이머 시작 시 `setTimeout` 대신 **서비스워커 `showNotification` + `timestamp` 트리거는 iOS 미지원이므로**, 단순하게: 타이머 시작 시점에 종료 시각 알림을 스케줄할 방법이 iOS PWA엔 없음 → **Push API 없이 가능한 최선은 visibilitychange 복귀 시 놓친 종료를 즉시 알리는 것 + 타이머 종료 시점에 앱이 포그라운드면 Notification 표시.** 조사 후 iOS 제약으로 실효가 없다고 판단되면 **구현하지 말고 이 항목에 조사 결과를 기록할 것** (짐작 구현 금지).

### 조사 결과 (2026-07-31) — **구현하지 않음**

**결론: 이 항목이 목표한 시나리오(휴식 중 폰을 내려놓음 = 화면 꺼짐/백그라운드)는 서버 없는 PWA에서 도달 불가능하다.** 구현하면 권한 요청 프롬프트와 설정 토글이 늘어나는데 실제 문제는 그대로 남는다.

미래 시각에 알림을 띄우려면 앱이 실행 중이 아닐 때 코드가 돌아야 한다. 경로가 셋뿐이고 전부 막혀 있다:

| 경로 | 상태 | 근거 |
|---|---|---|
| **Notification Triggers API** (`showTrigger` + `TimestampTrigger`) | 사용 불가 | 실측: `'showTrigger' in Notification.prototype` = false, `TimestampTrigger` 미정의. Chrome 실험 기능으로 출시되지 않고 제거됐고 Safari에는 애초에 없다 |
| **Web Push** | 사용 불가 | 푸시를 *보낼* 서버가 필요하다. DESIGN §1 비목적이 "서버 백엔드" 없음을 규정한다. 실측 환경에서 `pushManager`도 없었다 |
| **`setTimeout` + `showNotification`** | 무효 | 타이머 콜백은 **JS가 돌 때만** 실행된다. iOS는 PWA가 백그라운드/화면 잠금이면 JS를 정지시킨다 — 마일스톤 3에서 휴식 타이머를 endTime 타임스탬프 방식으로 만든 이유가 바로 이것이다. 화면이 꺼진 동안에는 알림을 띄울 코드가 실행되지 않는다 |

**구현 가능한 것과 그 무의미함**: 타이머 종료 시점에 앱이 **포그라운드**면 `Notification`을 띄울 수 있다. 그런데 그 상황은 사용자가 이미 화면을 보고 있는 경우이고, 하단 바가 초록색 "휴식 완료"로 바뀌고 비프음도 울린다. 알림은 정보를 하나도 더하지 않는다. 복귀(`visibilitychange`) 시점 알림도 같은 문제다 — 이미 앱을 보고 있으므로 화면의 "휴식 완료"로 충분하다 (마일스톤 3에서 구현 완료).

**검토했으나 기각한 대안 — Screen Wake Lock**: 휴식 동안 화면을 켜두면 비프음이 울린다. 하지만 (1) 사용자가 전원 버튼을 눌러 잠그면 해제되고, (2) 폰을 주머니에 넣는 동작 자체가 화면을 끄는 것이므로 목표 시나리오를 못 막고, (3) 배터리를 계속 소모한다. 실측에서도 `navigator.wakeLock.request('screen')`이 `NotAllowedError`였다(비활성 탭 제약).

**남는 한계**: 화면이 꺼져 있으면 휴식 종료를 알릴 방법이 없다. README Limitations에 이미 명시돼 있고, iOS PWA의 플랫폼 제약이다. 네이티브 앱이나 서버(Web Push)가 생기면 다시 검토할 항목.

> 실측 환경 주의: 위 API 검사는 이 개발 환경(Chromium/Electron)에서 수행했고 iOS Safari가 아니다. 다만 결론의 근거는 **"서버 없음 + Triggers API 부재"라는 플랫폼 무관한 구조적 제약**과 **iOS의 백그라운드 JS 정지**(마일스톤 3에서 이미 실측으로 확인한 동작)이므로 환경 차이가 결론을 바꾸지 않는다.

## T8. 대체운동 추천 + 무게·횟수 환산 (사용자 요구)

**시나리오**: 머신 자리가 안 나면 카드 메뉴에서 "자리 없음 → 대체운동"을 탭 → 후보 목록에서 선택 → 원 종목 기록을 근거로 시작 무게·목표 반복이 자동 설정된 카드로 교체.

### 환산 로직 — 검증된 부분과 휴리스틱을 분리한다

**⚠ 종목 간 하중 전이에 검증된 공식은 존재하지 않는다** (머신 표시 무게는 브랜드·지렛대 구조에 따라 실제 부하가 달라 원리적으로 불가). 검증된 것은 ① Epley 추정 1RM `w × (1 + reps/30)` ② %1RM-반복수 관계(rep max continuum)뿐이다. 따라서:

```
1. 대체 종목에 과거 기록 있음 → 그 recordKey 히스토리로 일반 프리필 (환산 안 함)
2. 처음 하는 대체 종목:
   a. e1RM_원 = 원 종목 최근 3세션 최고 세트의 Epley
   b. 시작무게 = roundToHalf(e1RM_원 × startFactor × 30/(30+repMax_원))
      — startFactor는 아래 표의 보수적 휴리스틱 (코칭 관행, 검증 아님 — 주석 명시)
   c. 첫 세트: "RIR 3~4에서 멈추세요" 안내 배지와 함께 수행
   d. 첫 세트 (무게 w₁, 반복 r₁) → e1RM_대체 = w₁ × (1 + r₁/30)  [검증된 부분]
   e. 2세트부터: 제안무게 = roundToHalf(e1RM_대체 × 30/(30+목표reps) × 0.95)
      (0.95 = RIR 1~2 여유. 목표reps = 원 종목 repMin~repMax 유지)
```

### 대체 후보 테이블 (exercises.json에 `substitutes` 필드로)

| 원 종목 | 대체 후보 | startFactor | 비고 |
|---|---|---|---|
| 인클라인 체스트프레스 | 스미스머신 인클라인 프레스 | 0.70 | 바 무게 포함 총량 기준 |
| " | 덤벨 인클라인 프레스 | 0.16 | **한쪽 덤벨 무게로 기록** (규칙 UI에 표시) |
| 체스트프레스(플랫) | 스미스머신 벤치프레스 / 덤벨 벤치프레스 | 0.70 / 0.16 | " |
| 펙덱 플라이 | 시티드 케이블 플라이(크로스오버) | 0.35 | 한쪽 스택 기준 |
| " | 덤벨 플라이 (플랫벤치) | 0.14 | 한쪽 덤벨 |
| 숄더프레스 머신 | 덤벨 숄더프레스 | 0.15 | 한쪽 덤벨 |
| 랫풀다운 | 어시스티드 풀업 머신 | 특수 | 보조무게 = 체중 − 랫풀무게×0.9 (역방향 — 클수록 쉬움) |
| 시티드 케이블 로우 | 체스트서포티드 로우 머신 | 0.80 | |
| " | 원암 덤벨 로우 | 0.30 | 한쪽 |
| 암 풀다운 | (대체 없음 — 케이블 하나는 비기 마련) | — | |
| 펙덱 리어델트 | 케이블 리어델트 플라이 | 0.25 | 한쪽 |
| 레터럴 / 컬 / 푸쉬다운 | 덤벨↔케이블 상호 | 기록 라인 공유 (이미 "덤벨 또는 케이블"로 정의됨) | |
| **하체 전 종목** | **대체 없음 (의도)** | — | 발 제약(스쿼트·런지·레그프레스 금지)상 안전한 대체가 없다. 순서 변경으로 해결 |

startFactor는 **첫 세트 시작점일 뿐이며 캘리브레이션(d~e)이 본체**다. 계수가 틀려도 RIR 3~4 첫 세트라 위험이 없고, 2세트부터는 실측 기반이 된다.

### 기록 규칙 (루틴 문서 2장 정합)

- recordKey는 대체 종목 자신의 것 (`smith-incline-press@d1`) — 원 종목 기록 라인을 오염시키지 않는다
- `SessionEntry.substituteFor?: RecordKey` 추가 — 원 종목 연결
- 내보내기 MD: `### 스미스 인클라인 (D1/A) 40kg (대체: 인클라인 프레스 — 자리 없음)`
- 원 종목의 프리필·증량 판정은 영향받지 않는다. **"원래 종목의 세트를 나중에 그대로 추가하지 않는다"** — 대체한 날은 그걸로 끝 (문서 규칙)
- 대체 종목들도 catalog에 정식 등록 (cueTip 포함 — T1 문안 스타일로 작성)

### UI

- 카드 우상단 메뉴(⋯) → "자리 없음 · 대체운동" → 후보 시트 (후보별 예상 시작 무게 표시)
- 선택 시 해당 entry가 대체 종목으로 교체되고 "대체" 뱃지 + 원 종목명 표시
- 첫 세트에 "캘리브레이션 — RIR 3~4" 배지, 첫 세트 체크 시 2세트 무게 자동 갱신 제안
- 3회 이상 같은 대체를 쓰면 "이 종목을 루틴에 고정할까요?" 제안은 **하지 않는다** — 종목 고정은 루틴 문서(v2.4) 개정 사안이지 앱이 결정할 일이 아님. 대신 통계에 대체 빈도 표시

**수용 기준**: 캘리브레이션 산식 단위 테스트 (Epley 왕복, 어시스트 풀업 역방향 케이스 포함). 대체 기록이 내보내기에 원 종목과 구분되어 나온다.

## T9. 종목별 무게 단위 (사용자 요구 — 머신 핀이 2.5kg가 아닌 경우)

**현황**: 증량 제안·스테퍼가 전역 `weightIncrementKg: 2.5` 고정. 한 핀이 5kg인 머신에서 "40 → 42.5" 같은 존재하지 않는 무게를 제안한다. **v2.4 문서 10장은 원래 "머신은 한 핀"으로 규정** — 앱이 종목별 핀 무게를 모르는 것이 구멍이다.

**변경**:
1. Dexie에 `exerciseSettings` 테이블 신설: `{ recordKey, weightStepKg?: number, weightLadderKg?: number[], note?: string }` — **T4(머신 세팅 메모)와 같은 테이블을 공유한다. T4와 함께 구현할 것** (스키마 version 2 한 번으로)
2. 카드 ⋯ 메뉴 → "무게 단위": [1 / 1.25 / 2 / 2.5 / 5 / 직접입력 / **불규칙(핀 목록)**]. 미설정 시 기본 2.5
3. **불규칙 스택 대응**: 핀이 균일하지 않은 머신(2.5씩 가다 3씩 등)은 실제 핀 값을 콤마 목록으로 등록 (`5,10,15,20,25,30,35,41,47`). `weightLadderKg`가 있으면:
   - 스테퍼 ±가 **사다리의 이웃 값으로 이동** (35에서 + → 41). 현재 값이 사다리에 없으면 가장 가까운 값으로 스냅 후 이동
   - 증량 제안 = 사다리에서 현재 무게의 **다음 값** (마지막 핀이면 제안 없음 — "스택 최대" 표시)
   - 직접 입력은 여전히 자유 (사다리는 스테퍼·제안에만 적용, 저장값 스냅 강제 없음)
4. 적용 지점 (전부): 스테퍼 step, `computeProgression`·`progressionSuggestions`, 내보내기 MD의 "다음: N kg로 증량"
5. 한 번 설정하면 recordKey에 영구 저장 — 같은 머신은 다시 설정할 필요 없음

**수용 기준**: 5kg 단위 종목의 증량 제안이 +5. 사다리 등록 종목은 스테퍼가 사다리를 따라 이동하고 제안이 다음 핀으로 뜬다 (경계: 최솟값에서 −, 최댓값에서 + / 사다리 밖 값에서 스냅). 단위 테스트 포함. JSON 백업에 exerciseSettings 포함.

## T10. B그룹 무게 회복 가이드 — Phase 2+ (리뷰어 판단 추가)

**근거**: 루틴 문서 9장 Phase 2 — "B그룹 무게를 조금씩 회복. **단, 감각 점수가 1로 떨어지면 즉시 이전 무게로 복귀. 절대 기준이다.**" 현재 앱은 B그룹에 아무 신호도 주지 않는다. Phase 0~1에서는 그게 맞지만(증량 금지), Phase 2부터는 문서의 절대 기준을 앱이 감시할 수 있다.

**변경** (`src/lib/bGroupGuide.ts` 신설, 파생 계산):
1. **회복 힌트** (Phase ≥ 2, B그룹만): 같은 recordKey에서 최근 3세션 연속 감각 점수 3 AND 마지막 세션 전 세트가 repMax 도달 → 카드에 "무게 회복 시도 가능 (+한 단위)" 힌트 칩. **프리필 기본값은 바꾸지 않는다** — A그룹과 달리 자동 상향 없음, 힌트만. B그룹 성공 기준은 기록이 아니라 감각이므로 결정은 사용자
2. **복귀 경고** (절대 기준의 자동화): 직전 세션보다 무게를 올린 세션에서 감각 점수가 1 이하로 기록되면 → 세션 요약 + 다음 세션 카드에 "감각 1점 — 이전 무게(N kg)로 복귀" 경고, 다음 세션 프리필을 **이전 무게로 자동 복귀**
3. Phase 0~1에서는 두 기능 모두 비활성 (문서: Phase 1 동안 B그룹 증량 금지)

**수용 기준**: 힌트·경고 판정 단위 테스트 (감각 3-3-3 + 상단 / 무게 상승 + 감각 1 / Phase 0 비활성). 경고 시 프리필이 이전 무게로 내려간다.

## T11. 반복 보상작용 감지 → 무게 하향 제안 (리뷰어 판단 추가)

**근거**: 루틴 문서 13장 — "세트 중 이게 보이면 **무게가 과하다**." 사용자가 보상작용을 성실히 기록해도(예: 리어델트 "목·승모 긴장") 현재 앱은 아무 반응이 없다. 같은 종목에서 같은 문제가 반복되는 건 그 자체가 무게 하향 신호인데, 이 판단을 사용자 기억에 맡기고 있다. 내보내기 → LLM 분석은 깊은 피드백 경로로 유지하되, **단순 반복 패턴은 앱이 즉시 잡는 게 맞다.**

**변경** (`src/lib/compensationWatch.ts` 신설, 파생 계산):
1. 같은 recordKey에서 **최근 3세션 중 2회 이상 보상작용이 "없음"이 아니면** → 다음 세션 그 카드에 경고 칩: "보상작용 반복 — 무게 하향 권장 (문서 13장)" + 기록된 문구 표시
2. 칩 탭 → 한 단위(T9의 step/사다리) 낮춘 무게를 원탭 적용하는 시트. **자동으로 낮추지는 않는다** — A그룹은 "자극 찾으려고 무게 내리지 않는다"가 원칙이라 하향은 항상 사용자 결정 (B그룹 감각 1점 복귀(T10)와 달리 이쪽은 강제 규칙이 아님)
3. 3세션 연속이면 홈 화면에도 요약 배너 (우선순위: 기존 배너들보다 아래)
4. 세션 요약(FinishSheet)에 이번 세션 보상작용 기록 종목 목록 표시 — 기록 직후 인지

**수용 기준**: 판정 단위 테스트 (2/3 충족, "없음"만 3회, 종목 간 독립). 경고 칩에서 원탭 하향 시 그 세션 프리필만 변경되고 기록 라인은 유지된다.

## T12. B그룹 무게·볼륨 차트 — Phase 2부터 조건부 노출 (사용자 요구)

**현황**: 분석 탭 추이 차트(무게×횟수/볼륨/e1RM)는 A그룹만 대상 (`strengthRecordKeys`). B그룹은 감각 점수 추이만 있다. **의도된 제한이었다** — B그룹 성공 기준은 감각이라, 무게 차트를 주면 무게를 쫓게 되는 역효과 우려.

**변경 근거**: Phase 2부터는 루틴 문서가 B그룹 "무게 회복"을 규정하므로 (9장, T10과 동일 근거) 그 시점부터는 무게 추이가 회복 진행 판단에 필요한 정보가 된다. 조건부 노출이면 원래 의도(Phase 0~1 무게 집착 방지)와 충돌하지 않는다.

**변경**:
1. `strengthRecordKeys`에 `includeB: boolean` 옵션 추가 — `currentPhase >= 2`일 때 B그룹 recordKey 포함
2. 분석 탭 종목 칩에 B그룹 구분 표시 (기존 `nameOfKey` 형식에 그룹 뱃지)
3. B그룹 차트 선택 시 안내 문구 고정 표시: **"B그룹 기준은 감각이다. 무게 추이는 회복 참고용 — 감각 점수가 1로 떨어지면 이전 무게로 복귀 (문서 9장)"**
4. B그룹 차트에는 감각 점수를 보조 정보로 병기 (툴팁에 해당 세션 감각 점수 포함 — 무게가 올랐는데 감각이 떨어지는 패턴을 한눈에)
5. Phase 0~1에서는 현행 유지 (B그룹 칩 자체가 안 보임)

**수용 기준**: Phase 1에서 B그룹 칩이 없고, Phase 2로 전환하면 나타난다. B그룹 차트 툴팁에 감각 점수가 뜬다. `strengthRecordKeys` 옵션 단위 테스트.

## T7. 워밍업 세트 타입 (선택 ★ — 여유 있을 때만)

**무엇**: 세트에 `warmup: boolean` 플래그. A그룹 첫 종목 전 가벼운 워밍업을 기록하되 볼륨·증량 판정·PR에서 제외. 시중 앱 표준. 루틴 문서에는 워밍업 규정이 없으므로 (암 풀다운 프라이머 제외) UI는 "세트 추가" 롱프레스 → "워밍업 세트로 추가" 정도로 숨겨둘 것.

---

## 작업 순서 권장

T2 (10분) → T1 (30분) → **T9+T4 (같은 테이블 공유 — 함께 구현)** → T3 (반나절) → **T8 (반나절)** → T11 → **T10+T12 (둘 다 Phase 2 조건부 — 함께 구현)** → T5 → T7.
T1·T2·T3·T8·T9·T12가 사용자 요구이므로 우선. T11은 T9의 무게 단위를 사용하므로 T9 뒤. T6은 조사 결과 구현하지 않기로 확정 (해당 섹션 참조). 각 T 완료마다 커밋 분리.

## 하지 않기로 한 것 (조사했고 제외)

- Apple Health 연동, 홈 위젯 — PWA에서 불가
- 운동 영상/GIF — 오프라인 우선 설계와 충돌, 종목 고정이라 필요성 낮음
- 소셜·공유·챌린지 — 1인용 앱
- 플레이트 계산기 — 머신 중심 루틴이라 무용
- 칼로리·식단 기록 — 설계상 비목적 (DESIGN §1), 인바디 앱과 중복
