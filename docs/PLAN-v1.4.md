# v1.4 — 실사용 피드백 3차 (2026-08-05)

> 사용자 보고 10건 중 코드 작업 9건 (#7은 질문 — 답변 완료, 단 그 과정에서 X7 갭 발견).
> X3이 이 라운드의 핵심이다 — 보고 2건(#3·#4)의 공통 뿌리이고 chokepoint 신설 건.

## ✅ X3. "근력 세션" 정의 chokepoint — #3·#4의 공통 뿌리 [핵심]

> **완료 (2026-08-06).** 테스트 548 → 568.
>
> **계획서의 적용 지점 4개보다 넓었다.** "그날 훈련했나"를 묻는 곳이 **네 곳**이었고
> 넷이 각각 `completedSessions(...).some(s => s.date === X)`를 계산하고 있었다 —
> 식단 정규화(useDiet) · 오늘 식단(DietScreen) · 과거 식단 편집(HistoryScreen) ·
> 홈 식단 칩(HomeScreen). 계획서는 첫 번째만 지목했다.
> 그래서 `strengthDates(sessions): Set<string>`를 함께 만들어 네 곳을 하나로 모았다.
>
> **홈에서 B2 결함의 사본을 하나 발견해 지웠다.** HomeScreen이
> `trained || (stored?.isTrainingDay ?? false)`로 **정규화를 다시 계산**하고 있었다 —
> 식단 라운드에서 `useDiet` 한 곳으로 모았다고 했던 그 정규화의 사본이다.
> `useDiet`가 이미 정규화한 값을 쓰도록 바꿔 계산 자체를 없앴다 (고치는 게 아니라 삭제).
>
> **기준 선택**: 작업 세트(`doneSets`, 워밍업 제외). 랙에 올려만 보고 그만둔 날은
> 감쇠할 회복도 훈련일 끼니의 근거도 없다. 테스트로 고정했다.
>
> **표시와 판정의 기준이 다른 것을 홈에 명시** (계획서 4번): 두 숫자가 **갈릴 때만**
> 한 줄 뜬다 — "유산소만 한 날도 불꽃으로 표시합니다 — 디로드·Phase 카운터는 근력 N회로
> 셉니다." 항상 적으면 잡음이고, 안 적으면 다른 숫자가 버그로 보인다.
>
> **브라우저 검증** (유산소만 한 세션을 오늘 날짜로 심고):
> 제안 D1 · 식단 휴식일 5슬롯 유지 · 잠금 없음 · 도트 🔥 표시 + 설명 줄 노출.
> **대조**: 같은 세션에 작업 세트 **1개만** 체크하면 제안 D4(감쇠 정상 발동) ·
> 식단 훈련일 6슬롯 고정 + 잠금 · 설명 줄 사라짐. 수정이 기능을 없애지 않았다.
>
> 인바리언트를 일부러 두 번 깨뜨려 확인했다 (판정을 completedSessions로 되돌리기 /
> 제안을 완료 세션 기준으로 되돌리기) — 각각 2건·4건 실패.
>
> ⚠ 조사 중 홈이 흰 화면이 됐는데 **내 손픽스처가 필수 필드(`compensation`)를 빼먹은**
> 탓이었다 (첫 커밋부터 있는 필드라 실제 백업에는 없을 수 없다). 앱 결함이 아니다 —
> 파생 함수를 하나씩 호출해 격리한 뒤 확인했다.

**보고**: ① D1 날 유산소만 했는데 다음날 D4 추천 (#3) ② 휴식일 선택했는데 유산소만 한
세션 때문에 훈련일로 강제 고정 (#4).

**원인 (확정 — 점수 재계산으로 검증)**: 두 코드 모두 "완료 세션"을 근력 수행으로 취급한다.
세트를 하나도 체크하지 않은(유산소만 기록한) 세션이:

- `suggestNextDay`의 **회복 감쇠**를 발동시킨다 — 어제 "D1 세션"이 있으므로 D1 점수에
  ×0.3 (실제로는 아무 근육도 안 썼는데). 주간 볼륨이 0일 때의 원점수는
  **D1 26.1 > D4 23.85 > D2 22.2** 인데, D1이 페널티로 7.8이 되면서 D4가 이긴다.
  사용자 직관("볼륨이 안 채워졌으면 D1 유지")이 정확히 맞고, 페널티 오발동이 원인이다
- `resolveTrainingDays`의 **훈련일 강제**를 발동시킨다 — "완료 세션 존재 = 훈련일"

**수정**: `derive.ts`에 chokepoint 신설 —

```ts
/** 근력 세트를 1개 이상 체크한 세션. 유산소만 기록한 세션은 근력 수행이 아니다 */
export function hasStrengthWork(session: Session): boolean
export function strengthSessions(sessions: Session[]): Session[]  // 완료 + 근력
```

적용 지점 (전부 — 한 곳이라도 빠지면 F1과 같은 부류의 재발):
1. `suggestNextDay` — 회복 감쇠의 `last`, 복귀 gap 판단, 하체 10일 가드(`daysSinceDay`)
2. `weekCounts` → 디로드 카운터·Phase 0 진행 (유산소만 간 날은 주 3회에 안 세는 것이
   문서 정합 — 문서의 "주 3회"는 근력 세션이다)
3. `resolveTrainingDays` (식단 훈련일 판정) — #4 해결. 유산소만 한 날은 휴식일 유지
4. 주간 도트·streak 표시는 **현행 유지** (헬스장에 간 것은 사실 — 불꽃은 유지하되
   카운터·판정만 근력 기준). 도트와 카운터의 기준이 달라지는 것을 홈 어딘가에 한 줄 명시

**테스트**: 유산소만 기록한 세션 픽스처로 — 다음날 제안이 D1(페널티 없음) /
weekCounts 미포함 / 식단 휴식일 유지 / 도트는 표시. + 인바리언트 파일에 한 줄 추가.

## ✅ X1. 세션 있는 날 기록 상세에 식단이 안 보임 (#1) [스펙 미구현]

> **완료.** 원인 확정: `pickDate`가 세션이 **하나뿐인** 날에는 곧바로 상세로 들어간다
> (`setDetailId`). 0개면 날짜 선택 → 식단 편집기, 2개 이상이면 목록 + 편집기.
> 그래서 정확히 "운동 하나 + 식단" 조합에서만 식단이 사라졌다.
>
> **요약을 새로 만들지 않았다** — `DietDayEditor`를 그대로 붙였다 (D3 원칙: 오늘·기록·상세가
> 같은 편집기). 편집기 자체가 상단에 요약(마크·단백질·슬롯 수)을 갖고 있어 계획서의
> "요약 + 편집 진입"이 한 번에 충족된다. 요약을 따로 만들면 식단 상태를 그리는 코드가
> 세 벌이 되고, 그게 이 프로젝트의 반복 결함이다.
>
> **계획서와 다르게 한 것**: "세션 여러 개인 날은 마지막 세션 상세 하단에만" 규칙을 두지
> 않았다. 한 번에 한 상세 화면만 보이므로 중복 표시가 생기지 않고, 한쪽에만 나오면
> "왜 여기선 안 보이나"가 된다. 대신 라벨에 날짜를 적어 **세션이 아니라 그 날짜의 식단**임을
> 드러낸다 (`2026-08-04 식단`).
>
> **브라우저**: 운동+식단 공존 날 상세에서 요약(단백질·235/1,781kcal·기록 1/6슬롯) ·
> 슬롯 6개 · 훈련일 잠김 · 삭제까지 전부 렌더.
>
> 부수 발견: X3의 정규화 인바리언트가 `DietDayEditor`를 **우연히** 통과시키고 있었다
> (`\btrained\b`가 `trainedThatDay`를 못 잡았다). 정규식을 넓히고, 남는 한 곳은
> **근거와 함께 예외 등록**했다 (기록 없는 날의 기본값이 필요하다). 우연한 통과가 예외보다 나쁘다.

PLAN-DIET §3이 "세션 상세 **아래** 식단 요약 + 편집"을 규정했는데 `SessionDetailScreen`에
식단 코드가 없다 (grep 0건). D3가 ✅ 처리됐지만 이 부분은 빠졌다 — 세션 없는 날만
DietDayEditor로 연결돼 "식단만 있으면 보이고 둘 다 있으면 운동만 보이는" 증상.

**수정**: SessionDetailScreen 하단에 그 날짜의 식단 요약(마크 + 슬롯 줄) + "식단 편집"
진입. 세션 여러 개인 날은 마지막 세션 상세 하단에만. 테스트: 세션+식단 공존 날 렌더.

## ✅ X2. 캘린더 식단 링 — 조사 후 표시 기준 완화 (#2)

> **완료.** 계획서의 진단이 맞았다 — 배선은 정상이고 `unlogged` 판정이 링을 없앴다.
> **실기기 확인 항목도 여기서 해소됐다**: 5슬롯 기록한 날의 초록 링이 브라우저에서
> 실제로 보인다 (`cal-cell-diet-good`, rgb(52,199,89)) — 스타일 문제가 아니었다.
>
> **`Adherence`에 값을 더하지 않았다.** 판정 기준(3슬롯)을 유지하라는 것이 요구였고,
> `good/mid/poor`의 의미가 바뀌면 과거 날의 마크가 전부 흔들린다. 그리고 이 정보는
> 판정이 아니라 **"기록이 있다"는 사실**이다 — 종류가 다른 것을 같은 열거형에 넣으면
> 마크·내보내기·칩까지 새 값을 처리해야 한다.
> 대신 `DietMonthStats.partialDates: Set<string>`를 냈다.
>
> 슬롯 수는 `summarizeDietDay`에서 가져온다 (여기서 세면 판정과 다른 기준이 생긴다).
>
> **브라우저(375px)**: 1슬롯·2슬롯 → 회색 링(`--text-faint`), 5슬롯 → 초록,
> 행만 있고 슬롯 0 → 링 없음. 범례 두 문장 노출.

**조사 결과**: 링은 배선돼 있다 (HistoryScreen→MonthCalendar `adherenceByDate` 전달 확인).
안 보이는 유력 원인은 **'unlogged' 판정** — 3슬롯 미만 기록한 날은 링 자체가 없다.
사용자의 최근 기록(아침만·아침+점심)이 전부 이 구간이라 "링 없음"으로 보인 것.

**수정**: 1슬롯 이상 기록 + 3슬롯 미만 = **회색 링** ("기록 중 — 판정 보류"). 판정(●◐○)은
현행 3슬롯 기준 유지. 범례를 월 요약 옆에 한 줄 (회색 = 기록 있음/판정 전).
실기기 확인: 3슬롯 이상 기록한 날 색 링이 실제로 보이는지 (안 보이면 스타일 문제 — 재조사).

## ✅ X7. 식단만 기록한 날은 자동 백업이 안 올라감 (#7 질문에서 발견)

> **완료.** 쓰기 chokepoint 두 곳(`mutateDietDay`·`removeDietDay`)에만 붙였다 —
> 화면마다 부르면 새 편집 경로를 추가할 때 빠뜨린다. 실제로 X1에서 세션 상세에 편집기를
> 하나 더 붙였는데 **아무것도 추가하지 않아도 백업이 동작한다.**
> **성공한 뒤에** 예약한다 (`.then`) — 실패한 쓰기로 백업을 올리면 없는 변경을 올리는 셈이다.
>
> **브라우저 검증** (가짜 토큰을 잠깐 넣고 8초 debounce 전에 취소 — 외부 요청 없음):
> 변경 → 예약 · 삭제 → 예약 · 60ms 간격 6연타 → 예약 하나(status `pending`)로 흡수.
>
> ⚠ 처음에 "배선이 안 됐다"고 판단했는데 **브라우저가 X7 수정 이전 모듈을 쓰고 있었다**
> (페이지가 수정 전에 로드됐고 동적 import가 그 인스턴스를 반환했다). 리로드 후 정상.
> 파일과 테스트를 먼저 확인해서 코드를 고치기 전에 걸렀다.

**실증**: `requestSync` 호출처가 SessionScreen(세션 종료) 한 곳뿐. 현재 gist 마지막 갱신이
8/4 00:34 — 이후 이틀의 식단 기록이 백업에 없다. 다음 세션을 마쳐야 같이 올라간다.

**수정**: 식단 쓰기 경로(useDiet의 write-through)에도 debounce `requestSync()` —
기존 8초 debounce + visibilitychange flush 인프라 그대로 재사용. 슬롯 연타에 과호출되지
않게 debounce가 흡수하는지 테스트.

## ✅ X5. 자동완성 실험 종결 (#5)

> **완료 — 종결 선언.** 실험대 결과를 README Limitations에 기록했다: 대조군을 포함해
> 여섯 조합 모두 제안이 떴으므로 **웹 속성으로는 막을 수 없다**로 확정.
>
> `NO_AUTOFILL`은 걷어내지 않았다. 실패한 것은 "iOS 연락처 제안 억제"라는 **목표**이고,
> `autoCorrect`·`spellCheck` 끄기는 한글 메모 입력에서 여전히 유용하며 데스크톱에서는
> `autocomplete="off"`가 실제로 듣는다. 목표 실패가 속성의 다른 효용까지 지우지는 않는다.
>
> 마지막 카드(readOnly → 포커스 시 해제)는 넣었다가 **DEV-RECORD-v1.4 §7.1 권고대로
> 지웠다** (사용자 결정, 2026-08-06). 기대 이익이 거의 0인데(해제가 한 macrotask 뒤라
> readOnly 창이 사실상 0) 그 경로에 "필드가 영구히 readOnly로 남아 입력 불가"라는
> 실패 모드가 있었다 — 운동 중 유일하게 타이핑하는 필드에 남길 값이 없다.
> **실기기 확인 4번(readOnly 반응감)이 목록에서 빠진다.**
> 되살리기를 막기 위해 "readOnly 트릭이 남아 있지 않다"를 테스트로 두고, 지우려면
> 그 이유를 다시 읽게 만들었다.
>
> ⚠ **아래는 지운 트릭의 기록이다** (같은 실수를 다시 하지 않기 위해 남긴다).
> 검증에서 위험한 실패를 하나 밟아 설계를 바꿨었다. 처음엔 `onFocus`에서만 해제했는데
> 필드가 **영구히 readOnly로 남았다** — 창에 OS 포커스가 없으면 `activeElement`는 설정되지만
> focus 이벤트가 배달되지 않는다. 그 상태의 사용자는 **아무것도 입력할 수 없다.**
> 연락처 제안보다 훨씬 나쁜 실패다. 그래서 마운트 직후 타이머로 **무조건 해제**하고
> `onFocus`는 보조 경로로만 남겼다 — 실험이 실패하는 방향은 "제안이 계속 뜬다"(현상 유지)여야 한다.
> 그 성질을 테스트로 못 박았다 (해제 경로가 둘 / rAF 금지 — rAF는 화면이 안 보이면 아예 안 돈다).
>
> **기대치를 솔직히 적어 둔다**: 해제가 한 macrotask 뒤에 일어나므로 readOnly 창이 사실상
> 0에 가깝다. iOS가 그 사이에 제안 부착을 건너뛸지는 반반이다. 실기기에서 여전히 뜨면
> 두 줄(`readOnly`·`onFocus`)을 지우고 끝낸다 — 그게 계획서가 정한 종결 조건이다.
>
> **브라우저**: 이벤트 없이 해제됨 · 입력·저장 정상 ("시트 3칸, 등받이 2" 저장 확인).

**실험 결과 판독**: 6칸 전부(대조군 F 포함, `nope`·`type=search`·`type=number` 전부) 제안이
떴다 = **이 iOS 버전의 QuickType 연락처 제안은 웹 속성으로 억제할 수 없다.** `NO_AUTOFILL`을
포함해 속성 레벨 처방은 전부 무효로 확정.

**남은 카드는 하나**: readOnly 기본 + onFocus 해제 트릭. **머신 세팅 메모 한 곳에만**
최후 시도 (운동 중 실제로 타이핑하는 유일한 텍스트 필드라서). 첫 탭 반응 지연 부작용이
사용성을 해치면 즉시 되돌리고 **"플랫폼 제약 — 수용"으로 종결 선언** (README Limitations 기록).
사용자도 "사용성을 크게 해치진 않는다"고 했으므로 여기서 실험을 끝낸다. 더 파지 않는다.

## ✅ X6. 식단 삭제 버튼 danger 스타일 통일 (#6)

> **완료.** `btn-danger` 적용 — 색이 `--danger`(#ff453a)로 세션 삭제와 같다 (computed 실측).
> 소스 스캔으로 "삭제 진입 버튼은 전부 btn-danger"를 못 박았다.

식단 "이 날 기록 지우기"에 세션 삭제와 같은 `btn-danger` 적용. 파괴적 동작의 시각 언어는
앱 전체에서 하나여야 한다.

## ✅ X8. 세트 행 버튼 줄바꿈 (#8)

> **완료.** `+ 워밍업` → `워밍업`, `자리 없음 · 대체` → `대체`. 줄인 라벨의 원래 뜻은
> `aria-label`에 남겼다. `.btn-sm`에 `white-space: nowrap`을 2차 방어로 걸었다.
>
> **브라우저(375px)**: 5개 버튼 전부 `nowrap`, 높이 **44px 동일**(터치 기준 충족),
> 총 328px로 뷰포트에 들어간다. 계획서의 "카드 메뉴로 옮기지 말 것"을 지켰다.
>
> ⚠ CSS 규칙은 **단위 테스트로 검사할 수 없다.** vitest에서 `.css`를 `?raw`로 읽으면
> Vite의 CSS 플러그인이 가로채 **빈 문자열**이 온다 (실측 length 0).
> 읽을 수 없는 대상을 검사하는 척하는 테스트는 항상 통과하거나 항상 실패하고 둘 다
> 거짓 신호다 — 그래서 CSS는 브라우저 computed style로 확인하고, 테스트에는
> **1차 방어인 라벨 길이**만 남겼다.

"+ 워밍업"·"자리없음·대체" 라벨이 좁은 화면에서 줄바꿈. **라벨 축약** ("워밍업" / "대체") +
`white-space: nowrap`. 그래도 넘치면 아이콘+툴팁 대신 **버튼을 카드 메뉴(⋯)로 이동하지
말 것** — 운동 중 접근성이 우선이다. 375px에서 확인.

## ✅ X9. 보상작용 시트 버튼 정리 (#9)

> **완료.** `current`가 빈 값이거나 `없음`이면 "없음으로 비우기"를 렌더하지 않는다.
> **저장된 값**으로 판단한다 — 편집 중 선택으로 판단하면 체크를 다 푼 순간 버튼이 사라져
> 되돌릴 수 없다. 그 조건을 테스트로 고정했다.
>
> **브라우저**: '없음' 상태 → [저장][취소]만. 값을 넣고 다시 열면 → [저장][없음으로 비우기][취소].

"없음으로 비우기"는 **기존 값이 '없음'이 아닐 때만** 노출 (지울 게 있을 때만 보이는 것이
버튼의 존재 이유를 설명한다). '없음' 상태에서는 [취소]만. 현행 유지 판정이 아니라 조건부
노출로 수정.

## ✅ X10. 진단·실험 화면 정리 (#10)

> **완료.** `AudioDiagnosticsPanel`·`AutofillLab` 삭제, 설정에서 제거, 개수 일치 테스트의
> 대조군 예외 제거. 기존 "진단" 카드(카탈로그·세션 수·홈 화면 앱·저장 공간·시드)는 유지.
>
> **계획서보다 하나 더 지웠다**: 패널이 쓰던 `beep.ts`의 진단 API
> (`audioDiagnostics`·`playWithSessionType`·`PROBE`)가 죽은 코드로 남았다.
> X10의 논리("살려두면 유지 대상만 는다")를 그대로 적용해 67줄을 걷어냈다.
> `audioSessionSupported`도 지웠다 — 테스트만 쓰고 있었고, 그 술어는
> `configureAudioSession`의 판정을 복제한 **두 번째 사본**이었다. 테스트는 술어 대신
> **효과**(type이 ambient가 되는가)를 단정하도록 고쳤다.
>
> **브라우저**: 설정에서 두 패널 사라짐 · 기존 진단 카드 유지 확인.

- **소리 진단**: 역할 완료 (원인 규명 종료 + 사용자 "정상" 확인) — **삭제.** 코드는 git
  이력에 있으니 재조사 때 되살리면 된다. 살려두면 설정이 길어지고 유지 대상만 는다
- **입력 자동완성 실험**: X5 종결과 함께 삭제 (같은 커밋)
- 개수 일치 테스트의 대조군 예외 등록도 함께 제거

---

## #7 질문 답변 (기록용 — 코드 작업은 X7만)

**백업 없이도 데이터는 기기(IndexedDB)에 영구 저장된다.** 매일 백업할 필요 없다 —
세션을 마치면 자동으로 올라간다 (X7 수정 후에는 식단만 기록한 날도).
백업의 유일한 목적은 **기기 분실·파손·초기화 대비**다. 전제 하나: 홈 화면 앱으로 쓸 것
(Safari 탭으로 열면 7일 미사용 시 iOS가 지울 수 있다 — 온보딩에서 안내한 그 규칙).

## 작업 순서

X3 (핵심 — 테스트 포함 반나절) → X1 → X7 → X2 → X6·X8·X9 (퀵윈) → X5 → X10.

## ✅ 라운드 완료 (2026-08-06) — 9건 전부

테스트 **548 → 596** · `npm run build` 통과 · 배포 라이브 번들 해시 일치.

| 커밋 | 항목 |
|---|---|
| `c486248` | X3 근력 세션 chokepoint (핵심) |
| `320d303` | X1 세션 상세 식단 · X7 식단 쓰기 백업 예약 |
| `9ed25b5` | X2 회색 링 · X6 danger · X8 라벨 축약 · X9 조건부 비우기 |
| `08cf1e4` | X5 실험 종결 · X10 진단 화면 정리 |

**계획서보다 넓었던 것 2건**
- X3: "그날 훈련했나" 판정이 **네 곳**에 있었다 (계획서는 한 곳 지목). `strengthDates`로 통일.
  홈에서 B2 결함의 사본(`trained || stored`)을 하나 발견해 **삭제**했다
- X10: 패널이 쓰던 `beep.ts` 진단 API 67줄이 죽은 코드로 남았다. 같은 논리로 제거

**계획서와 다르게 한 것 3건**
- X1: "여러 세션인 날은 마지막 상세에만" 규칙을 두지 않았다 (한 번에 한 상세만 보이므로
  중복이 없고, 한쪽에만 나오면 "왜 여기선 안 보이나"가 된다)
- X2: `Adherence`에 값을 더하지 않고 `partialDates` 집합을 냈다 (판정 기준 유지가 요구였다)
- X5: 해제를 focus 이벤트에만 의존하지 않게 바꿨다 (검증에서 필드가 영구 readOnly로
  남는 것을 밟았다 — 입력 불가는 허용될 수 없는 실패 방향이다)

**검증 도구에서 배운 것 2건 (코드에 반영)**
- `lib/sourceScan.ts`로 `stripComments` 추출 — 금지 표현을 주석에 예시로 적어 스캔이
  자기 설명을 위반으로 신고한 것이 두 번이다
- CSS 규칙은 단위 테스트로 검사 불가: vitest에서 `.css`의 `?raw`가 **빈 문자열**이다
  (실측 length 0). 읽을 수 없는 대상의 단정을 지우고 브라우저 computed style로 옮겼다

## 실기기 확인 (배포 후)

1. X3 — 유산소만 기록한 다음날 제안이 D1인지 + 식단 휴식일이 유지되는지
2. X1 — 운동+식단 공존 날: 기록 상세에서 둘 다 보이는지
3. X2 — 3슬롯 이상 기록한 날 색 링 / 1~2슬롯 날 회색 링
4. ~~X5 — 머신 세팅 메모 readOnly 트릭~~ **취소** (트릭을 지웠다 — 확인할 것이 없다)
5. X7 — 식단만 기록 후 앱 백그라운드 → 설정에서 "마지막 백업" 갱신 확인
